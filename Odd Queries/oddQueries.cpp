#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    int t;
    cin >> t;

    while(t--){
        int n, q;
        cin >> n >> q;

        vector<ll> psum(n + 1, 0);
        for(int i = 1; i <= n; i++){

        }
    }


    return 0;
}
